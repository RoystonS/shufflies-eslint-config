/* eslint-disable @typescript-eslint/no-unused-vars */

// The default eslint no-duplicate-imports rule will
// block this, but it's a desirable pattern.
import type { URLPattern } from "node:url";
import { URL } from "node:url";
